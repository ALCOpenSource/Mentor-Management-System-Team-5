import React from "react";
import cx from "classnames";
import styles from "./Login.module.scss";

const Login = () => {
  return <div className={cx(styles.loginContainer)}>Login</div>;
};

export default Login;
